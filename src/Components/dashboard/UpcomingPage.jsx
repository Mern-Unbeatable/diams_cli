const UpcomingPage = ({ title }) => (
  <div>
    <h2 className="text-2xl font-bold text-primary">{title}</h2>
    <p className="mt-2 text-sm text-primary/60">Upcoming</p>
  </div>
);

export default UpcomingPage;
